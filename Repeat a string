function repeatStringNumTimes(str, num) {
  // repeat after me
  var str2 = str;
  if(num >= 0){
      for(k=0;k<num-1;k++){
        str += str2;
      }
    
  }else{
    str = "";
  }
  return str;
}

repeatStringNumTimes("abc", 3);
